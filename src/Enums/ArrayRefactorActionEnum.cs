namespace SerialPersistanceLibrary.Enums
{
    /// <summary>
    /// An enum used to define action to take when calling
    /// ArrayExtensions.Refactor<T>( T Value, ArrayRefactorAction Action ) extension method.
    /// </summary>
    public enum ArrayRefactorAction
    {
        Add    = 0,
        Remove = 1
    }
}
